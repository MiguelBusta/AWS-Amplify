{
  "name": "your-app-name",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws": "^0.0.3-2",
    "aws-sdk": "^2.1397.0",
    "skd": "^2.0.0"
  }
}
